# Skills Repo Guide

- Treat each subdirectory as a self-contained skill.
- Keep the repo copy as the source of truth.
- Do not edit installed copies under `~/.codex` or `~/.claude` by hand.
- When adding a new skill, keep its `SKILL.md` small and put reusable logic in `scripts/`.

